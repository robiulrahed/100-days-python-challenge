"""
Problem 7: Terminal Practice
Task: Run version-check commands in the terminal (not inside a .py file).

This file documents the commands and expected output for reference,
since this task is meant to be run directly in the terminal, not as a script.
"""

# Commands to run in your terminal (Command Prompt / Terminal / Bash):
#
#   python3 --version
#   pip3 --version
#
# Example expected output (your exact version numbers may differ):
#
#   Python 3.12.4
#   pip 24.0 from /usr/lib/python3/dist-packages/pip (python 3.12)

# Explanation:
# - These commands confirm that Python and pip (Python's package installer)
#   are correctly installed and accessible from the terminal.
# - This is important because later chapters will use `pip install`
#   to add external libraries like requests, pandas, and BeautifulSoup.
# - If these commands fail, it usually means Python was not added
#   to the system PATH during installation.

print("See the comments above for the terminal commands and expected output.")
