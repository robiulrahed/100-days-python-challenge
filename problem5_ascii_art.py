"""
Problem 5: ASCII Art
Task: Print a simple triangle pattern using print() statements (no loops yet).
"""

print("*")
print("**")
print("***")
print("****")

# Explanation:
# - Since loops haven't been covered yet (they come on Day 11), each row
#   of the triangle is printed manually with its own print() statement.
# - Each line has one more asterisk (*) than the line before it.
# - Later, with a `for` loop, this same pattern can be generated
#   in just 2 lines of code instead of 4 separate print() calls.
